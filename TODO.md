# TODO — 設計改善と残作業

この文書は、2026-08-20時点の全体走査で確認した残作業を、依存関係と効果の
大きさで並べたものです。最初に、兵士が個々の意志を持って動く戦場表現の直近
実装プランを置き、その後にプロジェクト横断の設計負債を記載します。機能目標
そのものは`docs/spec/12-roadmap.md`、現在動く範囲は`README.md`を参照してください。

変更では、固定小数点による決定論、システムの適用順、状態ハッシュ、描画の
読み取り専用性を維持することを前提にします。

## 直近実装プラン — 戦場での個体挙動

### 現在地と完了条件

個人任務（近傍の敵との交戦、特定人物の追跡、地点占拠、地点防衛）、連続的な
隊形アンカー、歩行中の弾性間隔、空いたスロットへの局所的な詰め、兵士ごとの
命令反応遅延までは実装済みです。加えてB0の観測基盤——読み取り専用の指標
（`sim-core::metrics`）、固定seedの回帰シナリオ5件（`sim-core::regression`、
`sim-headless regress`）、兵士1人の判断を見るUIデバッグ表示——も入りました。
以降はこの土台を作り直すのではなく、知覚、意思決定、局所集団、混雑処理、
任務のライフサイクルを順に足します。各フェーズの前後で`regress`の数値を
並べて比較します。

最終的な完了条件は、次の状態です。

- 同じ部隊の全員が同じtickに同じ方向へ反転せず、性格、疲労、士気、周囲の
  状況に応じて異なる判断時刻と行動を取る。
- 戦闘のそばで理由なく整列待機する兵士が減り、参加、援護、持ち場の維持、
  退避のいずれかを説明できる。
- 整列時の密な間隔と移動時の歩行可能な間隔を両立し、前後に空間ができた兵士が
  物理的に通れる経路で詰める。
- 「敵を倒す」「人物を倒す」「場所を占拠する」「場所を守る」の4目的が、
  戦況変化に応じた複数の個人行動へ分解される。
- 同一seed・同一入力の状態ハッシュを維持し、兵数を増やしても目標性能を
  満たす。

### B0 — 観測基盤と回帰シナリオ（完了）

- [x] headless計測に、任務別の行動人数、移動開始tickの分布、同方向へ同時に
  動き始めた割合、戦闘近傍の非交戦人数、スロットからの距離、標的集中数、
  地点占拠率、防衛範囲からの離脱人数を追加する。
  → `sim-core::metrics`（`BattlefieldObserver`）と`sim-headless regress`。
- [x] UIのデバッグ表示で、選択兵士の任務、現在行動、行動スコア上位候補、
  目標地点、認識中の敵、隊形スロット、反応待ち時間を確認できるようにする。
  → `soldierDebugJson`と詳細パネルの「判断」欄。記録対象は選択中の1人だけで、
  判断そのものは変えない。
- [x] 次の固定seed回帰シナリオを追加する。→ `sim-core::regression::SCENARIOS`。
  - 正面衝突中の横を予備隊が通過する。（`reserve_passes_melee`）
  - 縦隊の前列に損耗が生じ、後列が穴を埋める。（`column_front_losses`）
  - 狭路を通過してから開けた場所で再整列する。（`defile_then_reform`）
  - 防衛地点へ敵が接近し、一部が陽動で通過する。（`guard_area_feint`）
  - 指揮官を追跡中に視界喪失または対象死亡が起きる。（`hunt_person_lost`）
- [x] 計測はシミュレーション状態を変更せず、同一seedで集計値と状態ハッシュが
  毎回一致することをテストする。→ `observation_does_not_change_the_simulation`、
  `runs_are_reproducible`。

受け入れ条件: 上記5シナリオを自動実行でき、後続フェーズの改善量と性能退行を
数値で比較できること。調整用の閾値はコードへ散在させず、意味のある単位名を
持つ設定値にする。→ 閾値は`MetricsConfig`に集約。`regress`は指標と
`state_hash`/`trace_hash`をJSONで、1 tickあたりの実測時間を標準エラーへ出す。

B0時点の基準値（このワークツリーでの実測、`--release`）:

| シナリオ | tick | 移動開始（同方向%） | 戦闘近傍の非交戦（最大） | スロット距離 平均/p90 | 標的集中 最大 |
|---|---|---|---|---|---|
| `reserve_passes_melee` | 900 | 425（68.9%） | 97 | 258 / 500 mm | 24 |
| `column_front_losses` | 700 | 120（100%） | 0 | 1014 / 2000 mm | 0 |
| `defile_then_reform` | 900 | 96（100%） | 0 | 432 / 750 mm | 0 |
| `guard_area_feint` | 900 | 233（74.6%） | 38 | 6392 / 8000 mm | 27 |
| `hunt_person_lost` | 700 | 80（71.2%） | 0 | 325 / 750 mm | 0 |

読み方（同じ実行の`start_histogram`は5 tick刻み）:

- 命令への第一波は全員が20 tick以内に、その過半が最初の5 tickに集中する
  （例: `guard_area_feint`は160人中138人が先頭バケット）。現在の散らばりは
  隊形目標のラッチだけが作っており、B2の行動候補と継続ヒステリシスで
  広げるべき余地がここに数値として出ている。
- `guard_area_feint`では占拠側58人のうち範囲内は12人（20.6%）に留まり、守備側
  50人中11人が守備範囲の外にいる（迎撃範囲まで出たのは最大2人）。B5の地点
  行動が改善すべき対象。
- `hunt_person_lost`は対象死亡後に追跡隊48人が`no_objective`のまま`Idle`になる。
  B5の任務ライフサイクルが埋めるべき穴を数値で示している。
- 20,000人ベンチは変更前後で同一の状態ハッシュと同等の速度（交互実行で
  25.5〜31.3 ms/tick、差は測定ノイズの範囲）。観測は既定では動かないため、
  性能ゲートには影響しない。

### B1 — 局所知覚と敵認識（完了）

- [x] 兵士ごとに、近傍の敵味方、正面・側面・背面の脅威、接近・離脱方向、
  局所密度、近くの味方の崩れを決定論的に集約する。
  → `sim-core::perception`（`LocalAwareness`）。接近・離脱は`is_closing`。
- [x] 「近くに敵がいる」と「近くで味方が戦っている」を分離し、援護可能な
  戦闘だけを個人行動の候補にする。→ `supportable_enemy`。攻撃系でない任務では、
  援護・自衛（3 m）・守備範囲の侵入者／接近中の敵だけが候補。
- [x] 人物追跡を全知状態から外し、最後に認識した位置、認識からの経過時間、
  捜索範囲を持たせる。対象死亡・視界喪失・到達不能時の打ち切り条件も定める。
  → `organization::HuntMemory` / `HuntState`。打ち切り後は直前の任務、無ければ
  現在地の`Hold`へ戻し、`ObjectiveComplete`/`ObjectiveAbandoned`を記録する。
- [x] 地点防衛では、防衛範囲への侵入者と接近中の敵を優先し、範囲から離れて
  いく敵を無制限に追跡しないよう追撃距離を設ける。→ 離脱中の敵は候補から外し、
  追撃限界は個人スロットではなく持ち場の中心から測る。
- [x] 知覚更新を空間インデックスと更新頻度で制限し、全兵士対全兵士の走査を
  導入しない。→ `CoarseIndex::scan_in_radius`（走査人数の上限つき）、更新は
  兵士ごとに位相をずらした8 tick周期（交戦中は2 tick）。

受け入れ条件: 接近する敵と離脱する敵で防衛兵の選択が変わり、人物を見失った
兵士が最後の認識地点を短時間捜索した後に元任務へ戻ること。側面・背面の脅威は
正面より反応が遅れるか、驚きとして士気へ影響すること。
→ `guards_pick_the_approaching_enemy_over_the_leaving_one`、
`hunt_person_gives_up_after_searching_the_last_known_position`、
`threats_from_behind_are_noticed_later_than_those_in_front`、
`being_taken_from_behind_costs_morale_once`。

B1時点の回帰指標（B0比）:

| シナリオ | 移動開始（同方向%） | 戦闘近傍の非交戦（最大） | スロット距離 平均/p90 | 標的集中 最大 |
|---|---|---|---|---|
| `reserve_passes_melee` | 516（65.6%） | 199 | 236 / 500 mm | 23 |
| `column_front_losses` | 120（100%） | 0 | 1014 / 2000 mm | 0 |
| `defile_then_reform` | 96（100%） | 0 | 432 / 750 mm | 0 |
| `guard_area_feint` | 221（74.2%） | 67 | 4510 / 8000 mm | 26 |
| `hunt_person_lost` | 80（71.2%） | 0 | 325 / 750 mm | 0 |

- 敵のいない`column_front_losses`と`defile_then_reform`は状態ハッシュがB0と
  完全一致。知覚は「敵がいる場面」以外の挙動を変えていない。
- `guard_area_feint`: 追撃限界超えが最大2人→0人、占拠率20.6%→30.0%、
  戦死18人→14人。守備兵が持ち場を離れなくなり、攻め手が範囲へ入れている。
- `hunt_person_lost`: 対象死亡後の48人が`no_objective`ではなく`Hold`になり、
  `hunt.target_down`として記録される。
- `reserve_passes_melee`: 戦闘近傍の非交戦が最大97人→199人へ増えた。隊列を
  離れる条件を絞った結果で、「戦闘のそばで待機する兵士を減らす」完了条件は
  B2の行動候補（持ち場維持・援護・後退の使い分け）で回収する。
- 20,000人ベンチ 28.9〜29.5 ms/tick（B0と同等、差は測定ノイズ）。決定論検証
  （5,000人×1,200 tick）も一致。

### B2 — 個人効用AIの行動候補拡張（完了）

- [x] 現在の命令追従・近傍戦闘参加に、少なくとも次の行動候補を追加する。
  → `IndividualAction` は8つ（命令追従・迎撃・援護・受け・持ち場維持・後退・
  離脱・再集結）。
  - 持ち場を維持する／隊形スロットへ戻る。（`HoldSlot`）
  - 自衛する／味方を援護する。（`Defend` / `Support`）
  - 不利な間合いから一歩退く／安全な方向へ離脱する。（`FallBack` / `Flee`）
  - 潰走する／軍旗または指揮官の近くで再集結する。（`Flee` / `Rally`）
- [x] 行動スコアへ、脅威、任務への寄与、距離、士気、疲労、規律、勇敢さ、
  自己保存、仲間意識を入力する。→ `score_actions`。任務（`Intent`）は最終目的
  として保持し、行動は手段として選ぶ。
- [x] 現在行動への継続ボーナス、最低継続時間、切替閾値を設け、毎tickの
  行動往復を防ぐ。→ `CONTINUATION_BONUS` / `MIN_COMMIT_TICKS` / `SWITCH_MARGIN`。
- [x] 命令反応遅延だけでなく、自衛や恐慌など即応すべき割り込みの優先順位を
  定義する。→ `must_reconsider`（自衛と恐慌は思考周期も継続時間も待たない）。
- [x] 同点時の選択規則を安定IDなどで明示し、乱数の呼び出し回数に依存する
  非決定性を避ける。→ 同点は列挙順、乱数は1判断につき常に2回。

受け入れ条件: 同じ命令下でも臆病な疲労兵、規律ある兵、孤立した兵、援護可能な
兵が異なる行動を選ぶこと。近接した2部隊がtickごとに前進・後退を繰り返さず、
一定時間は選択した行動を継続すること。
→ `soldiers_in_the_same_unit_choose_different_actions`、
`chosen_actions_are_held_for_a_while`、
`a_sudden_close_threat_interrupts_the_current_action`、
`broken_soldiers_rally_towards_the_banner`。

B2時点の回帰指標（B1比）:

| シナリオ | 生存 | 戦闘近傍の非交戦（最大） | スロット距離 平均 | 標的集中 最大 |
|---|---|---|---|---|
| `reserve_passes_melee` | 124→195 | 199→199 | 236→5912 mm | 23→9 |
| `column_front_losses` | 91→91 | 0→0 | 1014→1014 mm | 0→0 |
| `defile_then_reform` | 96→96 | 0→0 | 432→432 mm | 0→0 |
| `guard_area_feint` | 146→152 | 67→64 | 4510→5615 mm | 26→15 |
| `hunt_person_lost` | 79→79 | 0→0 | 325→325 mm | 0→0 |

- 崩れた兵が実際に離脱・再集結するようになったため、`reserve_passes_melee`の
  生存が124→195に増え、スロットからの平均距離も伸びた（逃げた兵はスロットに
  戻らない）。標的集中は23→9人へ下がり、1人へ群がる度合いが減った。
- `guard_area_feint`では守備隊の26人が迎撃を選び、攻め手の占拠率は30.0%→0%へ
  落ちた（守備側が範囲を渡さなくなった）。
- 敵のいない2シナリオは状態ハッシュがB0/B1と一致。行動候補は「敵がいる場面」
  以外の挙動を変えていない。

B2で入れた・変えたAI定数と、その理由・効果（B7の記録要件）:

| 定数 | 値 | 理由 |
|---|---|---|
| `ENGAGE_BASE` | 500 | 反応半径内の敵へ出ることの底上げ。無いと戦列が接触しても誰も最後の間合いを詰めず、2,000人会戦の白兵戦が事実上消える（melee 4→41 kills / 8,000 tick） |
| `ARRIVED_WITH_ENEMY_PENALTY` | 500 | 持ち場に着いたうえで敵が反応半径内にいるときの「立っているだけ」を割り引く |
| `CONTINUATION_BONUS` / `SWITCH_MARGIN` | 150 / 80 | 僅差での乗り換えを防ぐ。合計を大きくすると初期行動（命令追従）から誰も動かなくなる |
| `DEFEND_TRIGGER_MM` | 1,500 | 武器の間合いの内側。広げると接触前に足が止まり白兵戦が起きない |
| `FALL_BACK_TRIGGER_MM` | 2,500 | 受けるより手前から退けるようにして、怯えた兵と踏み込む兵を分ける |
| `INTRUDER_BONUS` / `CLOSING_BONUS` | 900 / 600 | 守備任務では迎撃が任務そのもの。命令追従を上回る重みが要る |
| 疲労の重み（迎撃） | `fatigue / 25` | 疲れきった兵が突っ込まないようにする |
| `startle_cooldown_ticks`（B1の驚き） | 200 | 混戦で敵が前後を出入りするたびに士気が削れ、接触した瞬間に全軍が崩れるのを防ぐ |

同時に見つけて直した不具合:

- 局所知覚が味方で走査枠を使い切り、密集陣形の兵士に「敵が見えない」状態が
  生まれていた（敵は`query_enemies_in_radius`で別に引くよう修正）。
- `LocalAwareness`の既定値が「0番の敵が目の前にいる」と読めていた。
- 側背の脅威に毎回気づき直すため、側面の敵へいつまでも反応できなかった。
- `CoarseIndex`にセルごとの陣営マスクを持たせ、敵探索が味方だけのセルを飛ばす
  ようにした（20,000人ベンチ 37→25 ms/tick、状態ハッシュは不変）。

### B3 — 小集団と仲間を意識した行動（完了）

- [x] Unit内に3〜8人程度の決定論的なbuddy groupを作り、近い仲間、局所的な
  先導役、援護対象を保持する。所属更新で部隊全体を毎回再編しない。
  → `sim-core::squads`（`SquadSystem` / `Squad`）。隊形スロット順に最大6人ずつ、
  所属は2秒周期でしか見直さない。先導役は規律・勇敢さ・忠誠から決まり、倒れたら
  次の者へ移る。
- [x] 個人の参加判定に、仲間の交戦、孤立距離、局所人数差、先導役の行動を
  加え、一斉突入ではなく小集団単位の参加・躊躇・後退を生む。
  → `score_actions`の`cohesion`項（仲間の参加・先導役・最初の一人の躊躇・
  重心からの孤立）。
- [x] 1人の敵へ必要以上の人数が群がらないよう、接敵面と攻撃可能位置から
  実効的な標的上限を決め、余剰兵は隣の敵、援護、持ち場維持へ振り分ける。
  → `MAX_ATTACKERS_PER_TARGET`（4人）。上限に達した敵は候補から外れる。
- [x] 戦死・潰走で仲間を失った場合、近隣の既存集団へ段階的に合流する。
  → `merge_small_squads`。動くのは3人を切った集団の生存者だけで、受け入れ側の
  所属は変わらない。

受け入れ条件: 戦闘参加が部隊全員の同時切替ではなく複数の小さな波として発生し、
一部の兵が援護や予備として残ること。損耗のたびに全員の所属や移動先が変わらない
こと。→ `losing_a_soldier_does_not_reshuffle_the_unit`、
`tiny_squads_merge_into_a_neighbour`、`leadership_passes_on_when_the_leader_falls`、
`soldiers_follow_their_squad_into_a_fight`、`only_a_few_soldiers_can_crowd_one_enemy`。

B3時点の回帰指標（B2比）:

| シナリオ | 生存 | 標的集中 最大 | 小集団数 | 平均人数 | 交戦中の集団 | 1 tickの最大参加人数 |
|---|---|---|---|---|---|---|
| `reserve_passes_melee` | 195→194 | 9→9 | 36 | 5.4 | 1 | 9 |
| `column_front_losses` | 91→91 | 0→0 | 15 | 6.0 | 0 | 0 |
| `defile_then_reform` | 96→96 | 0→0 | 16 | 6.0 | 0 | 0 |
| `guard_area_feint` | 152→152 | 15→10 | 27 | 5.6 | 11 | 5 |
| `hunt_person_lost` | 79→79 | 0→0 | 13 | 6.1 | 0 | 0 |

- 「1 tickに迎撃へ移る最大人数」は240人のシナリオで9人、160人のシナリオで5人。
  部隊全員の同時切替ではなく、小集団ごとの波になっている。
- 標的集中の最大は`guard_area_feint`で15→10人。上限4人は「同じ相手を選び直せる
  人」には効くが、既に組み合っている相手へ後から詰め寄る分は`combat`側の話なので
  完全には0にならない。B4の混雑処理で再度見る。
- `SQUAD_FIRST_MOVER_PENALTY`は1,000人会戦3 seedの比較で決めた（60で白兵死
  21〜28、200で11〜21）。躊躇を残しつつ戦列が噛み合う中間として120。

### B4 — 歩行間隔、空所補充、局所混雑（ほぼ完了）

- [x] 移動時の希望間隔を速度、向かい合う交通、地形幅、局所密度で変え、停止・
  整列に近づくにつれて現在の密な隊形間隔へ滑らかに戻す。
  → 部隊ごとの`march_blend`（permille）で歩行間隔と整列間隔を1.25秒かけて
  行き来する。速度以外の要素（向かい合う交通・地形幅・局所密度）は未反映。
- [x] 空いたスロットを単純な最近傍だけでなく、同じfileの後方、隣接file、
  役割適性の順で埋める。規律、疲労、混雑によって発見・移動開始を遅らせる。
  → `fill_front_vacancies`を書き直した。**前列の穴がまったく埋まっていなかった
  不具合の修正を含む**（スロット番号が大きいほど前列なのに、穴埋めを番号の
  大きい側から取っていた）。
- [x] 兵士同士がすり抜けずに詰められるかを局所経路で確認し、通路がない場合は
  一時待機または別スロットを選ぶ。→ `lane_is_clear`（中点に体があれば見送る）。
- [x] 対向・交差する味方部隊に短距離の通行レーンと譲り合いを導入し、部隊の
  重なりと押し戻しの連鎖を抑える。→ `yield_to_oncoming`（向かってくる味方には
  進行方向の右手へ寄る）。
- [x] 重装兵は前列、射撃兵は後列など、兵種に応じたスロット優先度を追加する。
  → `sort_slots_by_role`（部隊ごとに1回だけ。射撃兵と工兵は後列側へ）。
- [x] 隊形アンカーの補正、兵士ステアリング、衝突解決が互いに逆向きの速度を
  毎tick生成していないか計測し、補正の責務を整理する。
  → `push_conflicts`として計測（進行方向と逆に押し戻された延べ回数）。責務の
  整理そのものはP1-4のコンテキスト型導入に合わせて行う。

受け入れ条件: 前列の穴が後方から順に埋まり、兵士が他者を貫通したり一斉に横へ
スライドしたりしないこと。狭路では間隔を保って通過し、出口で前方から段階的に
再整列すること。→ `front_vacancy_is_filled_from_behind_one_rank_per_interval`、
`missile_troops_stay_out_of_the_front_rank`、
`friendly_soldiers_pass_each_other_instead_of_jamming`、
`marching_spacing_is_wider_than_formed_spacing`。

B4時点の回帰指標（B3比、スロットからの距離）:

| シナリオ | 生存 | 平均 | 最大（期間中） | 押し戻しの綱引き（1 tick最大） |
|---|---|---|---|---|
| `reserve_passes_melee` | 194→183 | 8874→4501 mm | 40423→25156 mm | 20 |
| `column_front_losses` | 91→91 | 1014→1351 mm | 4323→18839 mm | 4 |
| `defile_then_reform` | 96→96 | 432→429 mm | 1387→1689 mm | 0 |
| `guard_area_feint` | 152→151 | 4988→4966 mm | 18145→18153 mm | 8 |
| `hunt_person_lost` | 79→79 | 325→289 mm | 6869→1353 mm | 0 |

- 隊列の維持そのものは良くなった（`reserve_passes_melee`の平均が8.9 m→4.5 m）。
- `column_front_losses`の最大が伸びたのは、前列の穴が実際に埋まるようになって
  前へ出た兵が倒れ、崩れた兵が離脱するようになったため（15 mの離脱距離）。
- 残り: 希望間隔へ「向かい合う交通・地形幅・局所密度」を反映する分。狭路の
  通過そのものは`defile_then_reform`で崩れていない（平均429 mm）。

### B5 — 任務のライフサイクルと地点行動（完了）

- [x] 各任務へ「移動中」「展開中」「交戦中」「確保済み」「失敗」「帰還中」の
  状態を持たせ、成功・失敗・再評価条件を定義する。
  → `organization::MissionState` / `MissionProgress`。`tick_mission_state`が
  10 tick周期で更新し、変化は`CommandEventKind::MissionStateChanged`に残る。
- [x] 地点占拠を中心点への殺到ではなく、範囲内の味方戦力、敵戦力、滞在時間、
  配置の広がりで判定する。余剰兵は周縁警戒または予備へ回す。
  → 範囲内の味方が6割以上で5秒保てば「確保済み」。敵が範囲に入れば「交戦中」。
  区域に入りきらない人数（1人あたり4 m²で計算）は半径1.15倍の周縁へ配る。
- [x] 地点防衛に中心、半径、追撃限界、優先方向を持たせ、持ち場へ戻る条件を
  明示する。→ 追撃限界はB1、優先方向は`nearest_enemy_bearing`で最寄りの敵部隊へ
  正面を向ける。
- [x] 人物追跡で、対象死亡、逃走、長時間の視界喪失、到達不能が発生した際の
  代替任務を定義する。→ B1で実施（`HuntMemory`と`finish_hunt`）。
- [x] UIで地点半径、追撃限界、任務状態を可視化し、命令した結果を追跡可能に
  する。→ 兵士の詳細パネルに「任務の段階」「任務地点（中心・半径・追撃限界・
  範囲内の人数）」を表示。
- [x] 条件付き命令（敵接近時、損耗率、時刻、地点喪失時）は、この状態機械を
  トリガーとして後から接続できる境界にする。→ `MissionProgress`（段階と滞在
  開始tick、範囲内の味方・敵の人数）と`MissionStateChanged`事象がその境界。

受け入れ条件: 占拠兵が一点に重ならず範囲へ展開し、防衛兵が無限追撃せず持ち場へ
戻ること。完了不能になった人物追跡で兵士が停止し続けないこと。
→ `occupying_an_area_moves_through_its_mission_states`、
`surplus_soldiers_watch_the_perimeter`、`occupy_area_spreads_stable_slots_across_the_zone`、
`guard_area_intercepts_then_returns_to_the_post`、
`hunt_person_gives_up_after_searching_the_last_known_position`。

B5時点の任務段階（回帰シナリオの最終tick）:

| シナリオ | 段階の内訳 |
|---|---|
| `reserve_passes_melee` | 展開中2・交戦中1 |
| `column_front_losses` | 移動中1 |
| `defile_then_reform` | 移動中1 |
| `guard_area_feint` | 移動中1・展開中1・交戦中1 |
| `hunt_person_lost` | 移動中1・展開中1（追跡は`target_down`で終了し`Hold`へ） |

### B6 — 指揮官AIによる任務分解（完了）

- [x] 指揮官の意図を、部隊全体への単一目標ではなく、接敵部隊、援護、予備、
  側面警戒、地点保持などの小任務へ分解する。→ `decompose_attack`。予備と要点
  保持を**先に**取り分けてから、残りを主戦闘（正面・側面・突撃）へ回す。
- [x] 戦力差、地形上の要点、友軍の任務、損耗、士気をBlackboard経由で評価し、
  再評価周期と任務変更コストを設ける。→ 要点は`Blackboard::terrain_notes`
  （高所・渡河点・道）。同じ役目のまま出し直すのは20秒経ってからか、任務が
  失敗したときだけ（`should_reassign`）。
- [x] 同一tickに複数部隊が同じ目標へ集中しすぎないよう、必要戦力と既割当戦力を
  比較して割り当てる。→ 必要戦力は敵の1.5倍。届いたらそれ以上は送らず、
  足りないときだけ予備を切り崩す。
- [x] `critical_points`を地形・道路・渡河点・高所から抽出し、地点占拠と防衛の
  候補へ接続する。→ `terrain_notes_near`（司令部の周囲300 mを40 m間隔で標本
  抽出）。拾った要点は`GuardArea`の中心になる。

受け入れ条件: 指揮官が少数の予備と地点防衛を残しつつ主戦闘へ戦力を送り、戦況が
変わっても全部隊が同時に同じ新目標へ反転しないこと。
→ `an_attack_is_split_into_sub_missions`、
`units_are_not_reassigned_to_the_same_role_immediately`。

実測（`scenario --scenario agincourt_1415 --metrics`、t4000）: 任務の内訳は
待機1753・突撃2114・側面警戒300で、任務段階は移動中2・展開中7・交戦中1。
数で劣る側は予備を切り崩して主戦闘へ送るため、予備が残らないこともある
（必要戦力に届かないときの意図した挙動）。

回帰シナリオ5件の状態ハッシュはB5と完全一致（これらは単一部隊のシナリオで、
指揮官による分解が働く場面を含まないため）。

### B7 — 性能、決定論、調整のゲート（B0〜B6ぶん完了）

このゲートは最後にまとめて行わず、B0〜B6の各フェーズで満たします。

- [x] `soldier_ai`、知覚、空間検索、ステアリング、衝突解決をフェーズ別に計測し、
  個体数に対する計算量と一時アロケーションを記録する。
  → `World::tick_profiled` と `bench --metrics`。20,000人・300 tickの内訳
  （1 tickあたり）:

  | フェーズ | 時間 | 割合 |
  |---|---|---|
  | movement（操舵＋移動積分） | 12.5 ms | 43.9% |
  | collision（押し合い・転倒・地形追従） | 8.4 ms | 29.7% |
  | combat（騎兵・突撃・白兵・射撃） | 5.8 ms | 20.5% |
  | soldier_ai（B2の行動選択） | 0.72 ms | 2.5% |
  | perception（B1の局所知覚） | 0.59 ms | 2.1% |
  | formation（穴埋め・スロット・空間ハッシュ） | 0.28 ms | 1.0% |
  | squads / command / commander_ai / engineering | 各0.1 ms未満 | 1%未満 |

  B1〜B6で足した判断系（知覚・小集団・行動選択）は合計で約1.3 ms、全体の5%。
  支配的なのは以前からある移動と衝突解決で、次の性能作業はそこに向ける。
- [x] 現在の20,000人ベンチマークを基準値として保存し、まず20,000人で
  50 ms/tick以下を満たす。→ このワークツリーでは25〜31 ms/tick（変更前の
  ベースラインと交互実行して同等。TODO記載の55.963 ms/tickは別環境の値）。
  50,000人での測定は未実施。
- [x] 遠方・非接敵兵の知覚更新頻度を下げ、接敵時だけ詳細候補を評価する
  active/sleeping方針を導入する。→ 敵が見えていない兵士の知覚更新は8 tick→
  24 tick周期、交戦中は2 tick周期。
- [x] Rust単体テスト、シナリオ回帰、状態ハッシュ、Wasmビルド、Web smoke test、
  `clippy`を各フェーズの完了条件にする。→ B0〜B6の各段階で実行。
- [x] AI定数の変更には、変更理由、影響する指標、比較前後の固定seed結果を残す。
  → B2・B3の節に表として記載。

残り: 50,000人での測定と、`docs/spec/12-roadmap.md`の4倍速目標。移動・衝突の
最適化はP1-8（データ転送とボトルネックの測定）と同じ土俵なので、そちらで扱う。

### 推奨する実装・コミット順

1. B0のheadless指標と固定seed回帰シナリオ。（完了）
2. B1の局所知覚、地点防衛の追撃制限、人物追跡の最後の認識位置。（完了）
3. B2の行動候補、効用スコア、行動ヒステリシス。（完了）
4. B3のbuddy groupと標的集中の抑制。（完了）
5. B4の空所補充経路、歩行間隔、局所混雑。（完了。希望間隔の要素は一部残り）
6. B5の任務状態機械とUI可視化。（完了）
7. B6の指揮官による小任務への分解。（完了）
8. 各段階のB7性能ゲートを通過後、閾値をまとめてプレイテスト調整。
   （B0〜B6ぶんのゲートは通過済み。閾値のプレイテスト調整は未実施）

各コミットは、挙動変更、回帰テスト、必要なデバッグ表示を同じ単位に含めます。
大きな一括AI置換は行わず、固定seedシナリオの指標を見ながら1つずつ有効化します。

## P0 — 二重管理と境界の解消

### 1. `sim-data`を導入し、TOMLを実行時データの正本にする

- [ ] 武器、防具、陣形、品質、兵科、指揮官アーキタイプ、AI重み、工兵作業、
  シナリオのスキーマを整数単位で定義する。
- [ ] `data/*.toml`を検証して読み込む`sim-data`クレートを追加する。
- [ ] `organization::formation_def`、`scenario.rs`の大規模定数、`combat.rs`の装備値
  など、TOMLとの写しを段階的に削除する。
- [ ] Web向けには、ビルド時に検証済みバイナリまたは生成Rustを埋め込み、起動時の
  TOMLパース負荷と非決定的な解釈差を避ける。
- [ ] データ変更だけでheadlessとブラウザの両方へ同じ値が反映される統合テストを
  追加する。

理由: 現在は人間向けTOMLとRust定数を手で同期しており、「数値はデータへ出す」
という設計原則をまだ満たしていません。シナリオ追加やバランス調整で静かな
食い違いが起きる最大の要因です。

完了条件: 代表的な陣形・装備・シナリオをTOMLだけ変更したテストで、ネイティブと
Wasmが同じ検証済みデータを読み、手書きの対応定数が残っていないこと。

### 2. Rust / TypeScript境界を型付き・バージョン付きにする

- [ ] `combatStats`、`commandNodes`、`soldierDetail`、`battleSites`などの平坦配列に、
  1か所のレイアウト定義とバージョンを設ける。
- [ ] Rustの定義からTypeScript型・stride・field indexを生成するか、共有スキーマから
  両言語のコードを生成する。
- [ ] 地形の列挙値と効果テーブルも同じ生成経路へ移し、現在の文字列抽出による
  `check_terrain_effects.mjs`を構造的な検証へ置き換える。
- [ ] Workerメッセージを、初期化、操作命令、問い合わせ、スナップショットの4群へ
  分け、各メッセージの所有権と転送対象を明示する。
- [ ] 不明なメッセージや配列バージョンを黙って無視せず、UIへ互換性エラーを返す。

理由: 現在はRustの`Vec<i32>`等とTypeScriptの手書きindexが多数あり、型検査を
通っても並びの変更は検出できません。

完了条件: Rust側のフィールド追加・並び変更が、生成差分または互換性テストなしでは
CIを通らないこと。

### 3. リプレイ形式を永続化フォーマットとして設計する

- [ ] `Recording`へ`formatVersion`、`simVersion`、`snapshotVersion`、シナリオID、
  データセットのハッシュを保存する。
- [ ] 読み込み時にJSONを実行時検証し、範囲外のtick・ID・数値を拒否する。
- [ ] 古い`setFactionGoal`など互換用コマンドのサポート期限と移行関数を決める。
- [ ] プリセットの配列indexではなく安定IDを保存し、並び替えで壊れないようにする。
- [ ] 互換性を維持できない場合は、再生開始前に理由を表示する。

理由: 現在のTypeScript型はコンパイル時だけ有効で、ユーザーが読み込むJSONの検証や
バージョン情報がありません。シナリオindexの保存も追加・並び替えに弱い状態です。

完了条件: 現行、1世代前、不正入力のfixtureを持つテストで、再生・移行・拒否が
それぞれ期待どおりになること。

## P1 — 大規模モジュールの責務分割

### 4. `World::tick`の依存をコンテキスト型へ整理する

- [ ] 各システムへ渡す多数の配列・引数を、読み取り専用入力、当該システムの状態、
  出力イベントに分けたコンテキスト型へまとめる。
- [ ] `sim-core/src/lib.rs`をワールド所有権とtick順序の記述へ絞り、移動、地形作用、
  近接間合いなどの処理を責務別モジュールへ移す。
- [ ] `combat.rs`、`organization.rs`、`scenario.rs`、`commander_ai.rs`、
  `engineering.rs`の1000行超モジュールを、データ型・評価・適用・fixtureに分割する。
- [ ] `#[allow(clippy::too_many_arguments)]`を、Wasm ABIなど境界上必要な箇所以外から
  段階的に削除する。
- [ ] 分割の各段階で長時間の決定論fixtureの状態ハッシュを固定し、処理順の変化を
  検知する。

理由: 巨大な関数と多引数APIは、どのシステムがどの配列を読み書きするかを不明瞭にし、
将来の並列化や局所テストを難しくしています。

完了条件: `World::tick`からフェーズ順を一画面で読め、各システムのmutableな入力が
型として限定され、境界以外の`too_many_arguments`許可が大幅に減っていること。

### 5. サンドボックスと史実シナリオの初期化経路を統一する

- [ ] `main.ts`にある対称デモの兵数・配置・陣形定数を、通常のシナリオ定義へ移す。
- [ ] `deployBlock`、`addLineUnit`、`setFactionGoal`の用途を、初期化専用と互換再生用に
  分離する。
- [ ] 初期ワールドを構築する責務をWorkerへ集約し、`ready`後にメインスレッドから
  複数コマンドを送り直す手順をなくす。
- [ ] 初期化の途中で別シナリオが選ばれた場合に、古い生成結果を破棄できる
  generation IDまたはキャンセル機構を追加する。

理由: 現在は史実プリセットをRust/Workerが構築する一方、対称デモだけは`main.ts`が
`ready`後に段階配置します。録画開始点、エラー処理、再初期化の意味が二重です。

完了条件: すべてのワールドが単一の`init`要求と単一の`ready`応答で完全に配置済みと
なり、リプレイも同じ経路で再構築されること。

### 6. 地形生成・キャッシュのライフサイクルを独立させる

- [ ] 地形生成を専用Workerまたはキャンセル可能なジョブにし、Sim Workerの再初期化と
  分離する。
- [ ] IndexedDBへschema version、最終利用時刻、容量上限を持たせ、古い生成器の
  キャッシュを安全に失効・削除する。
- [ ] キャッシュキーへ生成アルゴリズム版とシナリオ整形版を含める。
- [ ] 5 km地形の熱浸食と優先度フラッドをプロファイルし、品質別のセル解像度・反復数を
  設計する。
- [ ] 生成中の進捗、キャンセル、失敗をUIへ通知する。

理由: 現在のキャッシュは再訪を高速化しますが、アルゴリズム変更、容量管理、連続する
シナリオ選択への明示的なライフサイクルが不足しています。

完了条件: 生成器更新後に古いキャッシュを読まず、連続切替で最後に選んだシナリオだけが
表示され、キャッシュが上限容量を超えないこと。

## P1 — 描画とスケール

### 7. 地形を標高付きメッシュにし、崖側面を実装する

- [ ] 現在の平面クアッドを、標高テクスチャまたはチャンクメッシュで変位させる。
- [ ] `cliff`ビットから側面ジオメトリを生成し、地面との継ぎ目と深度順を検証する。
- [ ] 人物、命令オーバーレイ、ミニマップの座標変換を同じ標高モデルへ合わせる。
- [ ] 遠距離LODではチャンク単位に簡略化し、近距離だけ側面を描く。

理由: 現在の崖は暗い縁取りだけで、地形の見た目と通行不能判定が一致しません。

完了条件: 崖の上・下にいる兵士の遮蔽と描画位置が視覚的に一致し、全ズーム域の
スモークテストとフレーム予算を満たすこと。

### 8. 並列化より先にデータ転送とボトルネックを測定する

- [ ] `sim-headless`のフェーズ別時間と、ブラウザのtick、snapshot copy、Wasm人物生成、
  GPU描画を別々に計測する。
- [ ] 20k / 50k体、各LOD、1x / 4xの基準値を機械可読な結果として保存する。
- [ ] `SharedArrayBuffer`の利用可否を実行時に判定し、transferable buffer版を
  正式なフォールバックとして維持する。
- [ ] 連続SoAで自動ベクトル化できる処理だけをSIMD候補にし、空間ハッシュのような
  gather中心処理へ無理に適用しない。
- [ ] threads版ではID範囲ごとの出力イベントを分離し、最後にID順適用して、
  単一スレッド版との全tickハッシュ一致を検証する。

理由: `simd128`フラグは有効ですが、手書きSIMDの試作は非連続アクセスのため逆に
遅くなっています。計測なしの並列化は複雑性だけを増やします。

完了条件: ボトルネック別の再現可能なベンチがあり、採用した最適化が実機で改善し、
単一・並列実行の状態ハッシュが一致すること。

## P2 — 型安全性・試験・運用

### 9. ID・単位・設定引数を型で区別する

- [ ] `SoldierId`、`NodeId`、`UnitId`、`StructureId`をnewtype化し、`u32`の取り違えを
  コンパイルエラーにする。
- [ ] mm、cm、m、tick、ms、permilleの境界変換を関数または型へ集約する。
- [ ] 配置、Wasm地形投入、カメラ設定を設定structへまとめ、位置引数の順序依存を減らす。
- [ ] 外部入力の`usize`変換、配列長、座標範囲を境界で検証する。

完了条件: 異なるID・単位の混在が型検査で落ち、境界以外の`as`変換と多引数APIが
明確に減っていること。

### 10. ブラウザ疎通確認を安定したE2Eへ育てる

- [ ] `smoke:ui`と`smoke:cache`をCIへ追加する前に、残る固定待ち時間を状態待ちへ置き換える。
- [ ] ボタンの日本語表示や画面上の推定座標ではなく、安定した`data-testid`と
  ワールド状態を使う。
- [ ] `smoke:performance`は通常PRと分け、定期実行または専用ランナーで傾向を保存する。
- [ ] Playwrightの終了処理を`try/finally`へまとめ、途中失敗でもブラウザとpreviewを
  確実に停止する。
- [ ] 単一HTML、通常配信、キャッシュ済み再訪の3経路で同じシナリオと状態ハッシュを
  照合する。

完了条件: タイムアウトや文言変更による偽陰性を抑えつつ、UI、キャッシュ、単一HTMLの
回帰がPR上で検出されること。

### 11. 受け入れ条件を継続計測へ変える

- [ ] 仕様書のM0〜M9について、「実装」「自動検証」「目標達成」の3状態を機械可読な
  表にする。
- [ ] 勝率、死因、会戦時間、準備時間効果、主要史実シナリオの残存数を許容範囲付きで
  記録する。
- [ ] 意図したバランス変更では基準値更新理由をレビューできるようにする。
- [ ] READMEへ固定の古い実測値を貼らず、CI成果物またはリリース時レポートを参照する。

完了条件: 「コードがある」ことと「仕様の受け入れ条件を満たした」ことを区別でき、
性能・バランスの退行が差分としてレビューできること。
